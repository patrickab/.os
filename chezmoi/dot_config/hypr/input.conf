# Personal input overrides — sourced after Omarchy's default/hypr/input.conf

input {
  kb_layout = de
}

# Override: only kitty (Alacritty/foot handled elsewhere or removed)
windowrule = match:class (kitty), scroll_touchpad 1.5
