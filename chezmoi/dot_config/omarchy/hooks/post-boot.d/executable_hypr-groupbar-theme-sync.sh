#!/bin/bash
# Applies the active theme's colors to the Hyprland groupbar on cold start
# (looknfeel.lua's static values are just a placeholder fallback).
# See dot_local/bin/executable_hypr-groupbar-theme-sync.sh.
exec hypr-groupbar-theme-sync.sh
